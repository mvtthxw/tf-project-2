services:
  app-fargate:
    build:
      context: ./app-fargate
    ports:
      - "8081:80"

  app-managed:
    build:
      context: ./app-managed
    environment:
      PARAMS_STORE: "LOCAL VALUE"
    ports:
      - "8082:80"
